package game.shapes.implementations.statistics;

import game.shapes.implementations.S;

public class StatS extends S {
    public StatS(int level) {
        super();
        initialize(level, 0, 127, 68);
    }
}
